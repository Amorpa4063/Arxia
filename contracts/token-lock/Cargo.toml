[package]
name = "token-lock"
version.workspace = true
edition.workspace = true
rust-version.workspace = true
license.workspace = true
description = "Token locking contract for Arxia (vesting, staking)"
authors = []

[dependencies]
arxia-core = { path = "../../crates/arxia-core" }
